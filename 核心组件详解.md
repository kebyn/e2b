# 核心组件详解

> 本文档是 **概念说明文档**：回答“这个组件是什么、在 E2B 里负责什么、上下游怎么协作”。
>
> 本文档不维护部署命令、参数全集或组件裁剪结论；这些内容分别由部署文档、参数文档和组件分析文档维护。
>
> 相关文档：
> - [`README.md`](./README.md)：仓库总入口与文档导航
> - [`私有化部署组件分析.md`](./私有化部署组件分析.md)：回答“这个组件能不能省、怎么替换”
> - [`启动参数详解.md`](./启动参数详解.md)：回答“这个组件有哪些运行时参数”
> - [`FeatureFlags私有化部署方案.md`](./FeatureFlags私有化部署方案.md)：Feature Flags 完整默认值与替代方案
> - [`不修改代码完整部署指南.md`](./不修改代码完整部署指南.md)：这些组件在手工生产部署中的落地方式

---

## 阅读导航

### 如果你想理解系统为什么依赖这些组件
- 直接看本文档

### 如果你想知道私有化时哪些组件可以去掉或替换
- 看 [`私有化部署组件分析.md`](./私有化部署组件分析.md#阅读导航)

### 如果你想查环境变量或启动参数
- 看 [`启动参数详解.md`](./启动参数详解.md#阅读导航)

---

## 1. 认证 Provider / OIDC / Ory

### 是什么

`2026.28` 的认证入口已经从 Supabase-only JWT secret 转为 provider-based 配置。API 和 Dashboard API 都通过 `AUTH_PROVIDER_CONFIG` 接收外部 JWT/OIDC issuer 配置；Dashboard API 另外使用 Ory 管理用户资料、外部身份和 profile 数据。

Supabase 仍可作为外部 OIDC/JWT provider 的一种实现，但不再是当前代码中的专用配置项。

### 在 E2B 中的用途

```
用户/服务请求 → 外部 OIDC/JWT Provider 签发 Token → E2B API/Dashboard API 验证 → 授权访问
                                      │
                                      └── Dashboard API 通过 Ory 管理用户资料/身份映射
```

1. **API Key / Admin Token**：保留 `X-API-Key`、admin token 等服务端认证路径。
2. **外部 JWT/OIDC**：`AUTH_PROVIDER_CONFIG.jwt[]` 声明 issuer、audience 策略和 JWKS discovery。
3. **Dashboard Profile**：Dashboard API 需要 `ORY_SDK_URL`、`ORY_PROJECT_API_TOKEN`、`ORY_ISSUER_URL`，并要求 Ory issuer 与 `AUTH_PROVIDER_CONFIG.jwt[].issuer.url` 匹配或可从单一 issuer 推导。
4. **用户识别**：认证层从 JWT claims / Ory identity 中解析用户身份，关联到 E2B 的 team、sandbox、template 等资源。

### 工作流程

```
┌─────────────┐     ┌────────────────┐     ┌────────────────────┐
│   Browser   │────▶│ OIDC/JWT IdP   │────▶│ E2B API / Dashboard │
│  / Client   │     │ Ory / Hydra 等 │     │ Auth Provider Verify│
└─────────────┘     └────────────────┘     └────────────────────┘
      │                       │                       │
      │ 1. Login / Client Cred│                       │
      │──────────────────────▶│                       │
      │                       │ 2. JWT Token          │
      │◀──────────────────────│                       │
      │ 3. Request + Bearer JWT                        │
      │───────────────────────────────────────────────▶│
      │                       │ 4. OIDC discovery/JWKS │
      │                       │◀──────────────────────▶│
      │ 5. Response                                    │
      │◀───────────────────────────────────────────────│
```

### 环境变量

| 变量 | 说明 |
|------|------|
| `AUTH_PROVIDER_CONFIG` | 外部 JWT/OIDC provider 配置，JSON 结构为 `{"jwt":[...]}` |
| `CLIENT_PROXY_OIDC_ISSUER_URL` | API Edge gRPC 用于校验 Client Proxy token 的 issuer URL |
| `ORY_SDK_URL` | Dashboard API 访问 Ory 的 SDK/API URL |
| `ORY_PROJECT_API_TOKEN` | Dashboard API 访问 Ory 的 Project API Token |
| `ORY_ISSUER_URL` | Dashboard/Ory 对应的 OIDC issuer URL |

### `AUTH_PROVIDER_CONFIG` 示例

```json
{
  "jwt": [
    {
      "issuer": {
        "url": "https://auth.example.com",
        "audiences": ["e2b-api"],
        "audienceMatchPolicy": "MatchAny"
      },
      "cacheDuration": "30m"
    }
  ]
}
```

### 支持的认证方式

在 `packages/auth/pkg/auth/` 和 API/Dashboard API 路由中组合使用：

```go
auth.NewApiKeyAuthenticator(...)              // X-API-Key header
auth.NewAccessTokenAuthenticator(...)         // Bearer access token
auth.NewAuthProviderAuthenticator(...)        // Bearer JWT/OIDC provider
auth.NewAuthProviderTeamAuthenticator(...)    // JWT/OIDC provider + team ID
auth.NewAdminTokenAuthenticator(...)          // 管理员 token
```

---

## 2. Nomad

### 是什么
HashiCorp Nomad 是一个工作负载编排器，类似 Kubernetes。E2B 使用 Nomad 来**管理和调度 Orchestrator 节点**。

### 在 E2B 中的用途

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   E2B API   │────▶│    Nomad    │────▶│ Orchestrator│
│             │     │  (Cluster)  │     │   Nodes     │
└─────────────┘     └─────────────┘     └─────────────┘
      │                    │                    │
      │ 1. Query nodes     │                    │
      │───────────────────▶│                    │
      │                    │                    │
      │ 2. Node list       │                    │
      │◀───────────────────│                    │
      │                    │                    │
      │ 3. gRPC call to specific node          │
      │─────────────────────────────────────────▶
```

### 主要功能

1. **节点发现**：API 通过 Nomad 查询可用的 Orchestrator 节点
2. **工作分配**：Nomad 管理 Orchestrator 和 Template Manager 的部署
3. **健康检查**：Nomad 监控节点健康状态
4. **自动恢复**：节点故障时自动重启任务

### 使用 Nomad 的代码示例

```go
// packages/api/internal/orchestrator/client.go
func (o *Orchestrator) getNodes(ctx context.Context) ([]*node, error) {
    options := &nomadapi.QueryOptions{}
    // 从 Nomad 获取所有 Orchestrator 节点
    nomadNodes, _, err := o.nomadClient.Nodes().List(options.WithContext(ctx))
    // ...
}

// packages/api/internal/clusters/discovery/nomad.go
func ListOrchestratorAndTemplateBuilderAllocations(
    ctx context.Context,
    client *nomadapi.Client,
    filter NomadQueryFilter,
) ([]Allocation, error) {
    // 列出所有 Orchestrator 和 Template Builder 的分配
}
```

### 环境变量

| 变量 | 必需 | 默认值 | 说明 |
|------|------|--------|------|
| `NOMAD_ADDRESS` | ❌ | `http://localhost:4646` | Nomad API 地址 |
| `NOMAD_TOKEN` | ❌ | - | Nomad ACL Token（生产环境需要） |

### 本地开发模式

在本地开发时，可以跳过 Nomad：
- 不设置 `NOMAD_ADDRESS`，使用默认值
- API 会降级使用本地节点发现
- 适用于单节点测试

---

## 3. LaunchDarkly (Feature Flags)

### 是什么
LaunchDarkly 是一个功能开关（Feature Flags）服务，允许在**不重新部署代码**的情况下动态控制应用行为。

### 在 E2B 中的用途

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  LaunchDarkly│     │  E2B API /  │     │  Runtime    │
│  (Dashboard) │────▶│  Orchestrator│────▶│  Behavior   │
└─────────────┘     └─────────────┘     └─────────────┘
      │                    │                    │
      │ 1. Set flag        │                    │
      │  max-sandboxes=50  │                    │
      │                    │                    │
      │                    │ 2. Read flag value │
      │                    │───────────────────▶│
      │                    │                    │
      │                    │ 3. Apply limit: max 50 sandboxes
```

### 在 E2B 中主要控制什么

LaunchDarkly 在这里主要承担三类职责：

1. **性能调优**：例如每节点 sandbox 上限、GCS 并发、NBD 连接数、prefetch worker 数
2. **功能开关**：例如持久卷、P2P 传输、自动恢复、按 label 调度
3. **动态配置**：例如 Firecracker 版本映射、限流配置、出口流量限制、chunker 配置

### 为什么它和普通环境变量不同

- 环境变量更适合“启动时确定”的静态配置
- LaunchDarkly 更适合“运行时可调整”的策略和开关
- 它支持按 **team / template / sandbox** 维度做差异化控制

> 完整 Flag 列表、精确默认值、YAML/Unleash 替代方案，请看：[`FeatureFlags私有化部署方案.md`](./FeatureFlags私有化部署方案.md)

#### 6. 按上下文的目标控制

```go
// 支持按 team、sandbox、template 等维度进行差异化配置
ctx = featureflags.AddToContext(ctx,
    featureflags.TeamContext(teamID),           // 按团队
    featureflags.SandboxKind,                   // 按 sandbox
    featureflags.TemplateContext(templateID),   // 按模板
)
```

### 使用示例

```go
// 读取布尔标志
if flags.BoolFlag(ctx, featureflags.PeerToPeerChunkTransferFlag) {
    // 启用 P2P 传输
}

// 读取整数标志
maxSandboxes := flags.IntFlag(ctx, featureflags.MaxSandboxesPerNode)

// 读取 JSON 标志
versions := flags.JSONFlag(ctx, featureflags.FirecrackerVersions)
```

### 环境变量

| 变量 | 必需 | 说明 |
|------|------|------|
| `LAUNCH_DARKLY_API_KEY` | ❌ | LaunchDarkly SDK Key。未设置时使用默认值（离线模式） |

> 完整配置参考请看：[`启动参数详解.md`](./启动参数详解.md)；完整 Feature Flags 方案请看：[`FeatureFlags私有化部署方案.md`](./FeatureFlags私有化部署方案.md)

### 离线模式

当未配置 `LAUNCH_DARKLY_API_KEY` 时：
- 使用代码中的 `fallback` 默认值
- 适用于本地开发和测试
- 不支持动态更新

---

## 4. Redis

### 是什么
Redis 是一个高性能的内存键值存储。E2B 使用 Redis 作为**分布式缓存和状态存储**。

### 在 E2B 中的用途

```
┌─────────────────────────────────────────────────────────────┐
│                         Redis                               │
├─────────────────────────────────────────────────────────────┤
│  sandbox:{id}:catalog  →  Sandbox 位置和状态（路由用）      │
│  sandbox:{id}:state    →  Sandbox 运行状态                  │
│  template:{id}:cache   →  模板缓存元数据                    │
│  snapshot:{id}:cache   →  快照缓存元数据                    │
│  team:{id}:sandboxes   →  团队的 sandbox 列表               │
│  events:sandbox        →  Sandbox 事件流（Streams）         │
│  rate:limit:{key}      →  速率限制计数器                    │
│  peer:registry         →  节点注册表（P2P 发现）            │
└─────────────────────────────────────────────────────────────┘
```

### 主要数据结构

#### 1. Sandbox Catalog（路由表）

```go
// 用于 Client Proxy 查找 sandbox 所在节点
type SandboxCatalogEntry struct {
    SandboxID       string    `json:"sandbox_id"`
    ExecutionID     string    `json:"execution_id"`
    Address         string    `json:"address"`      // Orchestrator 地址
    TemplateID      string    `json:"template_id"`
    TeamID          string    `json:"team_id"`
    // ...
}

// 存储: sandbox:catalog:{sandboxID}
// TTL: 与 sandbox 生命周期一致
```

#### 2. Sandbox 状态存储

```go
// 用于 API 管理 sandbox 状态
// Keys:
//   sandbox:state:{teamID}:{sandboxID}  →  sandbox 数据
//   sandbox:expiration                  →  过期时间 ZSet
//   team:sandboxes:{teamID}             →  team 的 sandbox 集合
```

#### 3. 模板/快照缓存

```go
// 用于 Orchestrator 缓存模板元数据
// 避免频繁查询数据库
cache.NewRedisCache[*TemplateInfo](RedisConfig[*TemplateInfo]{...})
```

#### 4. 事件流（Redis Streams）

```go
// 用于异步事件处理
event.NewRedisStreamsDelivery[SandboxEvent](redisClient, "sandbox-events")
```

#### 5. P2P 节点注册表

```go
// 用于 Orchestrator 之间发现彼此
peerclient.NewRedisRegistry(redisClient, nodeAddress)
```

### 架构图

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│ Client Proxy│────▶│    Redis    │◀────│ Orchestrator│
│             │     │  (Catalog)  │     │   Node A    │
└─────────────┘     └─────────────┘     └─────────────┘
      │                    ▲                    │
      │ Query sandbox      │                    │
      │ location           │ Register           │
      │                    │ sandbox            │
      ▼                    │                    ▼
┌─────────────┐            │            ┌─────────────┐
│   Sandbox   │            │            │   Sandbox   │
│   (HTTP)    │◀───────────┼───────────▶│   (gRPC)    │
└─────────────┘            │            └─────────────┘
                           │
                    ┌─────────────┐
                    │  Orchestrator│
                    │   Node B    │
                    └─────────────┘
```

### 降级模式

当 Redis 不可用时：

```go
// packages/client-proxy/main.go
if redisClient != nil {
    catalog = NewRedisSandboxesCatalog(redisClient, featureFlagsClient)
} else {
    // 降级为内存 catalog（仅单实例有效）
    catalog = NewMemorySandboxesCatalog()
}
```

### 环境变量

| 变量 | 必需 | 说明 |
|------|------|------|
| `REDIS_URL` | ❌ | 单节点连接串，格式：`host:port` |
| `REDIS_CLUSTER_URL` | ❌ | 集群连接串（优先于 REDIS_URL） |
| `REDIS_TLS_CA_BASE64` | ❌ | TLS CA 证书（base64 编码） |
| `REDIS_POOL_SIZE` | ❌ | 连接池大小（默认 API: 160, Proxy: 40, Orch: 10） |

### 连接模式选择

```go
// packages/shared/pkg/factories/redis.go
switch {
case config.RedisClusterURL != "":
    // Redis Cluster（GCP Memorystore 推荐）
    redisClient = redis.NewClusterClient(clusterOpts)
case config.RedisURL != "":
    // 单节点 Redis
    redisClient = redis.NewClient(opts)
default:
    // Redis 禁用
    return nil, ErrRedisDisabled
}
```

---

## 5. Volume Token

### 是什么
Volume Token 是一个 JWT（JSON Web Token），用于**授权访问持久卷**。它类似于 AWS S3 的 Presigned URL，但用于 E2B 的持久卷系统。

### 在 E2B 中的用途

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Client    │────▶│   E2B API   │────▶│  Orchestrator│
│             │     │ (Issue JWT) │     │ (Verify JWT) │
└─────────────┘     └─────────────┘     └─────────────┘
      │                    │                    │
      │ 1. Create volume   │                    │
      │───────────────────▶│                    │
      │                    │                    │
      │ 2. Volume Token (JWT)                  │
      │◀───────────────────│                    │
      │                    │                    │
      │ 3. Mount volume + Token                │
      │─────────────────────────────────────────▶
      │                    │                    │
      │                    │  4. Verify token   │
      │                    │◀───────────────────│
      │                    │                    │
      │ 5. Access granted  │                    │
      │◀─────────────────────────────────────────│
```

### Token 结构

```json
{
  // JWT Header
  "alg": "ES256",
  "typ": "JWT",
  "tokid": "local-dev-2026-03-03"  // 密钥名称

  // JWT Claims
  {
    // 标准字段
    "aud": "cluster-id",           // 受众（集群 ID）
    "exp": 1711334400,             // 过期时间
    "iat": 1711330800,             // 签发时间
    "iss": "local.e2b.dev",        // 签发者
    "jti": "unique-token-id",      // 唯一 ID
    "nbf": 1711330800,             // 生效时间
    "sub": "team-uuid",            // 主题（团队 ID）

    // 自定义字段
    "teamid":  "team-uuid",        // 团队 ID
    "volid":   "volume-uuid",      // 卷 ID
    "voltype": "persistent"        // 卷类型
  }
}
```

### 签发流程

```go
// packages/api/internal/handlers/volume_token.go
func generateVolumeContentToken(config cfg.VolumesTokenConfig, volume queries.Volume, team *types.Team) (string, error) {
    claims := jwt.MapClaims{
        "aud":     clusterID.String(),
        "exp":     jwt.NewNumericDate(expiration),
        "iat":     jwt.NewNumericDate(now),
        "iss":     config.Issuer,
        "jti":     uuid.NewString(),
        "nbf":     jwt.NewNumericDate(now),
        "sub":     team.ID.String(),
        "teamid":  team.ID.String(),
        "volid":   volume.ID.String(),
        "voltype": volume.VolumeType,
    }

    token := jwt.NewWithClaims(config.SigningMethod, claims)
    token.Header["tokid"] = config.SigningKeyName
    return token.SignedString(config.SigningKey)
}
```

### 密钥类型说明

| 类型 | 算法 | 密钥格式 | 适用场景 |
|------|------|----------|----------|
| `ECDSA` | ES256 (P-256) | PEM 私钥 | **推荐**：体积小、性能好 |
| `RS256` | RSA 2048+ | PEM 私钥 | 兼容性好，体积大 |
| `HMAC` | HS256 | 任意字节 | 简单，需安全分发 |
| `EdDSA` | Ed25519 | PEM 私钥 | 最新最快，部分系统不支持 |

### 密钥生成示例

```bash
# ECDSA（推荐）
openssl ecparam -name prime256v1 -genkey -noout -out private.pem
openssl ec -in private.pem -pubout -out public.pem

# RSA
openssl genrsa -out private.pem 2048
openssl rsa -in private.pem -pubout -out public.pem

# 配置环境变量
VOLUME_TOKEN_SIGNING_KEY="ECDSA:$(base64 -w0 private.pem)"
```

### 环境变量

| 变量 | 必需 | 默认值 | 说明 |
|------|------|--------|------|
| `VOLUME_TOKEN_ISSUER` | ✅ | - | 签发者标识，如 `e2b.dev` |
| `VOLUME_TOKEN_SIGNING_METHOD` | ✅ | - | JWT 签名算法 |
| `VOLUME_TOKEN_SIGNING_KEY` | ✅ | - | 签名密钥，格式：`TYPE:base64(PEM)` |
| `VOLUME_TOKEN_SIGNING_KEY_NAME` | ✅ | - | 密钥名称，用于密钥轮换 |
| `VOLUME_TOKEN_DURATION` | ❌ | `1h` | Token 有效期 |

### 密钥轮换流程

```bash
# 1. 生成新密钥
openssl ecparam -name prime256v1 -genkey -noout -out new-key.pem

# 2. 更新配置（同时保留旧密钥用于验证）
VOLUME_TOKEN_SIGNING_KEY="ECDSA:$(base64 -w0 new-key.pem)"
VOLUME_TOKEN_SIGNING_KEY_NAME="prod-2026-03-25"

# 3. 等待旧 token 过期（默认 1 小时）

# 4. 验证新 token 正常工作后，移除旧配置
```

### 安全考虑

1. **密钥存储**：生产环境应使用 GCP Secrets Manager 或类似服务
2. **有效期控制**：`VOLUME_TOKEN_DURATION` 应根据使用场景调整
3. **集群隔离**：`aud` 字段验证 token 只在特定集群有效
4. **团队隔离**：`teamid` 字段确保 token 只访问团队自己的卷

---

## 6. 制品仓库（Artifacts Registry）

### 是什么
制品仓库用于存储模板构建后的 Docker 镜像，供 Orchestrator 和 Template Manager 拉取使用。

### 在 E2B 中的用途

```
┌─────────────┐     ┌─────────────┐     ┌─────────────────────┐
│Template     │────▶│  Build      │────▶│ Artifacts Registry  │
│Manager      │     │  Process    │     │ (GCP/AWS/Local)     │
└─────────────┘     └─────────────┘     └─────────────────────┘
       │                                        │
       │ 拉取镜像                               │
       └────────────────────────────────────────┘
```

### 支持的提供者

| 提供者 | 环境变量值 | 说明 |
|--------|-----------|------|
| Google Cloud Artifact Registry | `GCP_ARTIFACTS` | GCP 部署默认 |
| Amazon ECR | `AWS_ECR` | AWS 部署默认 |
| 本地 Docker | `Local` | 私有化部署、单机部署 |

### 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `ARTIFACTS_REGISTRY_PROVIDER` | `GCP_ARTIFACTS` | 制品仓库类型 |

### 私有化部署建议
- 单机部署：使用 `Local`，直接从本地 Docker daemon 拉取
- 集群部署：使用 `AWS_ECR` 或自建 Harbor/Docker Registry

---

## 7. Sandbox 网络分配

### 是什么
E2B 为每个 Sandbox 分配独立的 IP 地址，用于 host 和 VM 内部通信。

### 网络架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Sandbox 网络                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Host Network CIDR: 10.11.0.0/16                            │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Slot 1: 10.11.0.1/32  (Host IP - host 访问 sandbox) │    │
│  │ Slot 2: 10.11.0.2/32                                 │    │
│  │ ...                                                  │    │
│  │ Slot N: 10.11.0.N/32  (支持 ~65534 个 sandbox)      │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                             │
│  Vrt Network CIDR: 10.12.0.0/16                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Slot 1: 10.12.0.0/31  (vpeer + veth, 2 IPs)         │    │
│  │ Slot 2: 10.12.0.2/31                                 │    │
│  │ ...                                                  │    │
│  │ Slot N: 10.12.0.(2N)/31                              │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `SANDBOXES_HOST_NETWORK_CIDR` | `10.11.0.0/16` | Host IP 网络 |
| `SANDBOXES_VRT_NETWORK_CIDR` | `10.12.0.0/16` | Vrt（vpeer/veth）网络 |
| `USE_LOCAL_NAMESPACE_STORAGE` | `false` | 使用本地 IP 分配（替代 Consul） |

### IP 分配机制
- **Host IP**：每个 slot 分配一个 `/32` IP，用于 host 直接访问 sandbox
- **Vrt IP**：每个 slot 分配一个 `/31` 子网（2 IPs），用于 sandbox 内部 vpeer 和 veth 通信
- **存储方式**：
  - `USE_LOCAL_NAMESPACE_STORAGE=false`：通过 Consul 分布式分配（需要 `CONSUL_TOKEN`）
  - `USE_LOCAL_NAMESPACE_STORAGE=true`：本地内存分配（私有化推荐）

---

## 组件依赖关系

```
┌─────────────────────────────────────────────────────────────────┐
│                        E2B Architecture                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────┐      ┌──────────┐      ┌──────────────────────┐  │
│  │Auth/Ory  │      │Discovery │      │   LaunchDarkly       │  │
│  │OIDC/JWT  │      │Nomad/K8s │      │   (Feature Flags)    │  │
│  └────┬─────┘      └────┬─────┘      └──────────┬───────────┘  │
│       │                 │                       │               │
│       ▼                 ▼                       ▼               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      E2B API                            │   │
│  │  - 用户认证 (AUTH_PROVIDER_CONFIG / OIDC JWT)            │   │
│  │  - 节点发现 (SERVICE_DISCOVERY_PROVIDER)                 │   │
│  │  - 功能控制 (LaunchDarkly)                              │   │
│  └─────────────────────────┬───────────────────────────────┘   │
│                            │                                    │
│              ┌─────────────┼─────────────┐                     │
│              │             │             │                     │
│              ▼             ▼             ▼                     │
│        ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│        │  Redis   │  │   gRPC   │  │  Volume  │              │
│        │ (Cache)  │  │ (Direct) │  │  Token   │              │
│        └──────────┘  └──────────┘  └──────────┘              │
│              │             │             │                     │
│              └─────────────┼─────────────┘                     │
│                            │                                    │
│                            ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Orchestrator                         │   │
│  │  - Sandbox 管理                                         │   │
│  │  - 模板构建                                             │   │
│  │  - 卷挂载 (Volume Token 验证)                           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 组件职责总结

| 组件 | 职责 | 是否必须 |
|------|------|----------|
| **Auth Provider / Ory** | JWT/OIDC 认证、Dashboard 用户资料 | ⚠️ Dashboard/用户级认证需要 |
| **Service Discovery** | Orchestrator/Template Manager 发现；支持 Nomad/Kubernetes/local | ✅ 多节点部署需要 |
| **LaunchDarkly** | 功能开关、动态配置 | ❌ 未配置时使用默认值 |
| **Redis** | 缓存、状态存储、事件流 | ❌ 未配置时降级为内存 |
| **Volume Token** | 持久卷访问授权 | ✅ 使用持久卷时必须 |

---

*文档同步至上游 e2b-dev/infra 仓库 tag 2026.28*
