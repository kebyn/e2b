# 上游同步说明：e2b-dev/infra 2026.17 → 2026.28

> **目标版本**: 上游 `e2b-dev/infra` tag `2026.28` (`fda7bef1095afb909197e272c0a8a123797f0bfb`)
>
> **历史比较基线**: 上游 `2026.17` (`9c3b7c5dbd181ba819084276b8228f70936e911e`)
>
> **当前状态**: `infra/` gitlink 和本仓库 E2B 中文文档已统一到目标版本。本文保留版本差异作为历史背景，并记录已完成范围和可复现验证。

---

## 1. 同步结论

从 `2026.17` 到 `2026.28` 不是一次纯文档更新。上游变更规模为：

```text
1296 files changed, 89279 insertions(+), 24909 deletions(-)
```

主要影响面集中在：

| 目录 | 变更文件数 | 主要影响 |
|------|------------|----------|
| `infra/packages/orchestrator` | 404 | microVM、暂停/恢复、模板构建、存储/cache、调试文档 |
| `infra/packages/api` | 194 | 认证、服务发现、调度、模板/快照/volume API |
| `infra/packages/db` | 148 | migration、认证 identity、team/env/template/snapshot 查询 |
| `infra/packages/shared` | 136 | feature flags、grpc/shared models、遥测与通用逻辑 |
| `infra/packages/envd` | 71 | VM 内 daemon 协议和运行时行为 |
| `infra/tests/integration` | 63 | 集成测试覆盖更新 |
| `infra/packages/dashboard-api` | 59 | Ory/OIDC、认证用户、dashboard 后端配置 |
| `infra/iac/provider-gcp` | 34 | GCP Terraform provider、Nomad cluster、Redis、镜像/网络 |
| `infra/packages/auth` | 31 | 通用认证 provider、OIDC/Ory 相关逻辑 |
| `infra/iac/modules` | 30 | Nomad job modules、API/proxy/orchestrator/template-manager job |

对本地中文文档的核心结论：

- `2026.17` 只在本文历史比较中保留，不再作为当前部署依据。
- 私有化认证文档已使用 `AUTH_PROVIDER_CONFIG` + Ory/OIDC/JWT provider 抽象。
- 服务发现文档已区分 `nomad`、`kubernetes`、`local` 三个合法 provider。
- Client Proxy 与 API 文档已使用 internal/edge gRPC 当前变量，旧变量只保留在历史差异中。
- Feature Flags 清单已按 `infra/packages/shared/pkg/featureflags/flags.go` 校准为 80 项。
- DB 与 ClickHouse migration 已纳入当前部署步骤，升级仍必须先备份并单独执行 migration。

---

## 2. 官方部署入口变化

`infra/self-host.md` 在 `2026.17 → 2026.28` 的关键变化：

| 主题 | `2026.17` | `2026.28` | 文档影响 |
|------|-----------|-----------|----------|
| Terraform | 要求 `1.5.x` / `1.5.7`，并说明 MPL/BUSL 原因 | 要求 `1.7.5` | `infra/self-host.md` 和中文部署文档中的 Terraform 版本说明需更新 |
| Postgres | 示例偏 Supabase connection string | 只要求 IPv4-compatible Postgres connection string | “Supabase DB only” 的措辞需降级为历史背景或移除 |
| GCP secrets | 包含 `e2b-supabase-jwt-secrets` | 移除此项 | 官方路径不再要求 Supabase JWT secret 作为 dashboard 必填项 |
| AWS env template | 曾写 `.env.template` | 改成 `.env.aws.template` | AWS 部署入口已按当前文件名核对 |
| AWS Firecracker 主机要求 | `bare metal instances` | `nested virtualization` | AWS 节点规格说明需更新 |
| AWS prerequisites | 包含 `gsutil` | 删除 `gsutil` | AWS 部署不再把 gsutil 作为官方前置条件 |
| public builds | kernels/rootfs/Firecracker | kernels、Firecracker versions、busybox | 复制公共构建产物说明需更新 |
| cluster prep | `make prep-cluster` in `infra/packages/shared`，`make seed-db` in `infra/packages/db` | `infra/` 内根级 `make prep-cluster` / `make seed-db` | 初始化命令位置需更新 |

上游还新增了 `infra/AGENTS.md`、`infra/docs/ARCHITECTURE.md`、`infra/docs/RELEASING.md`，并更新 `infra/CLAUDE.md`、`infra/packages/orchestrator/README.md`，这些可作为后续维护本地 agent 指令和架构说明的参考来源。

---

## 3. 配置与运行时变化

### 3.1 `.env.gcp.template`

新增或调整的关键项：

- `DOCKER_REVERSE_PROXY_VERSION`
- `CLIENT_PROXY_VERSION`
- `CLICKHOUSE_MIGRATOR_VERSION`
- `GCP_REDIS_NODE_TYPE`
- `CLIENT_PROXY_OIDC_ISSUER_URL`
- `ORY_SDK_URL`
- `ORY_ISSUER_URL`

删除的旧 dashboard 开关：

- `ENABLE_AUTH_USER_SYNC_BACKGROUND_WORKER`
- `ENABLE_BILLING_HTTP_TEAM_PROVISION_SINK`

说明变化：

- Postgres 注释从 Supabase 示例收敛为普通 Postgres connection string。
- Ory Project API token 由 cloud secret 管理，模板只留下说明，不直接要求明文写入 env。

### 3.2 `.env.aws.template`

新增关键项：

- `DOCKER_REVERSE_PROXY_VERSION`
- `CLIENT_PROXY_VERSION`
- `CLICKHOUSE_MIGRATOR_VERSION`

这些变量意味着上游支持从 E2B artifact registry 拉取已发布的预构建镜像，再镜像到自有 registry；本地“从源码构建所有服务”的文档需要明确这是可选路径。

GCP/AWS 模板还新增了 sandbox firewall 的 `ALLOW_SANDBOX_INTERNAL_CIDRS` 注释说明，但在 `2026.28` 模板中它是注释行，不是实际赋值变量。

### 3.3 API 配置

`infra/packages/api/internal/cfg/model.go` 的关键变化：

- 新增 `CLICKHOUSE_CONNECTION_STRINGS`，支持用分号分隔多个 ClickHouse 连接串。
- 新增 `SERVICE_DISCOVERY_PROVIDER`，可选：
  - `nomad`：默认，查询 Nomad HTTP API。
  - `kubernetes`：查询 K8s in-cluster API。
  - `local`：使用静态 orchestrator 地址。
- Nomad discovery 新增：
  - `NOMAD_ORCHESTRATOR_SERVICE_NAMES`
  - `NOMAD_ORCHESTRATOR_LEGACY_DISCOVERY_ENABLED`
- Local discovery 新增：
  - `LOCAL_ORCHESTRATOR_ADDRESS`
- K8s discovery 新增：
  - `K8S_NAMESPACE`
  - `K8S_ORCHESTRATOR_POD_LABEL_SELECTOR`
  - `K8S_TEMPLATE_MANAGER_POD_LABEL_SELECTOR`
- API gRPC 端口从单一 `API_GRPC_PORT` 拆成：
  - `API_INTERNAL_GRPC_PORT`，默认 `5009`
  - `API_EDGE_GRPC_PORT`，默认 `5109`
- 新增 `CLIENT_PROXY_OIDC_ISSUER_URL`。
- `SUPABASE_JWT_SECRETS` 被 `AUTH_PROVIDER_CONFIG` 取代。
- `DEFAULT_KERNEL_VERSION` 不再在 API config 中兜底。
- `SANDBOX_STORAGE_BACKEND` 从 API config 中移除。
- Volume token 新增 `VOLUME_TOKEN_ENABLED`，当设为 `false` 时可不提供 signing env。

### 3.4 Client Proxy 配置

`infra/packages/client-proxy/internal/cfg/model.go` 的关键变化：

- 旧 `API_GRPC_ADDRESS` 拆分为：
  - `API_INTERNAL_GRPC_ADDRESS`
  - `API_EDGE_GRPC_ADDRESS`
- 新增 edge gRPC OAuth：
  - `API_EDGE_GRPC_OAUTH_CLIENT_ID`
  - `API_EDGE_GRPC_OAUTH_CLIENT_SECRET`
  - `API_EDGE_GRPC_OAUTH_TOKEN_URL`

这会影响所有手工部署脚本、Nomad job、systemd unit 和 Nginx/proxy 文档中的 API/proxy 通信说明。

### 3.5 Dashboard API 配置

`infra/packages/dashboard-api/internal/cfg/model.go` 的关键变化：

- 新增 `CLICKHOUSE_CONNECTION_STRINGS`。
- `SUPABASE_JWT_SECRETS` 替换为 `AUTH_PROVIDER_CONFIG`。
- 删除 `SUPABASE_DB_CONNECTION_STRING`。
- 删除：
  - `ENABLE_AUTH_USER_SYNC_BACKGROUND_WORKER`
  - `ENABLE_BILLING_HTTP_TEAM_PROVISION_SINK`
- 新增 Ory 配置：
  - `ORY_SDK_URL`
  - `ORY_PROJECT_API_TOKEN`
  - `ORY_ISSUER_URL`
- 新增 `DOMAIN_NAME`。
- 启动校验要求：
  - 必须配置 `REDIS_URL` 或 `REDIS_CLUSTER_URL`。
  - 必须配置 Ory SDK URL、Project API Token、Issuer URL。
  - 当 `AUTH_PROVIDER_CONFIG.jwt[]` 存在时，`ORY_ISSUER_URL` 必须匹配其中一个 issuer。

---

## 4. 认证体系变化

本地旧文档中多处将 Supabase 描述为 E2B 私有化认证的中心组件。按 `2026.28`，这需要调整：

- API 和 Dashboard API 主要以 `AUTH_PROVIDER_CONFIG` 接收认证 provider 配置。
- `infra/packages/auth` 新增/强化了 OIDC/Ory 相关逻辑。
- 官方 self-host secrets 已移除 `supabase-jwt-secrets` 的必填说明。
- Dashboard API 需要 Ory profile/admin SDK 相关配置。

已完成校准：

- `核心组件详解.md` 已将 Supabase 降为历史/可选 provider 背景，并说明 `AUTH_PROVIDER_CONFIG`、OIDC/JWT/Ory 的当前抽象。
- `私有化部署组件分析.md` 已按“选择认证 provider”说明 API Key-only 与 Dashboard/Ory 两类部署边界。
- `启动参数详解.md` 已记录 `AUTH_PROVIDER_CONFIG`、`ORY_*`、`CLIENT_PROXY_OIDC_ISSUER_URL` 及 Dashboard 启动校验。
- `不修改代码完整部署指南.md` 已移除无法由固定 `infra` tag 验证的旧 Supabase Dashboard Frontend 示例。

---

## 5. 服务发现与部署拓扑变化

`2026.28` 引入更明确的服务发现抽象：

| Provider | 变量 | 适用场景 |
|----------|------|----------|
| `nomad` | `SERVICE_DISCOVERY_PROVIDER=nomad` | 官方 Nomad 部署路径，默认值 |
| `kubernetes` | `SERVICE_DISCOVERY_PROVIDER=kubernetes` | K8s 部署路径，通过 pod label selector 查找服务 |
| `local` | `SERVICE_DISCOVERY_PROVIDER=local` | 本地开发或静态单 orchestrator |

这会影响：

- `K8s私有化部署指南.md` 已使用上游 Kubernetes discovery，不再把 K8s 描述为必须自行改代码适配。
- `不修改代码完整部署指南.md` 已补充 Nomad service names 和 legacy discovery 开关。
- `私有化部署组件分析.md` 已将 K8s 和 local 作为当前源码支持项重新评估。
- `启动参数详解.md` 已记录 discovery 相关变量和默认值。

---

## 6. 用户可见能力变化

### 6.1 Sandbox Host 与 Header 寻址

`2026.17` 的共享代理解析器已经包含 `E2b-Sandbox-Id` 与 `E2b-Sandbox-Port` Header 基础，但 Client Proxy 和 Orchestrator 只在 local 环境启用 Header 解析，因此不能把 Header 名称本身表述为 `2026.28` 首次加入。本次版本区间新增的是生产可用的入口和解析范围：

- `345fa2452`：引入稳定 `sandbox.<DOMAIN_NAME>` 共享 Host，并限定共享 Host 与普通 Sandbox Host 的解析优先级。
- `f50b5f281`：允许 localhost 以外的 IPv4/IPv6 Host 使用 Header 路由，覆盖 bridge、LAN、on-prem 和 e2b-local 等无法把目标编码进 Host 的场景。
- `orch-accepts-combined-host`：控制 Client Proxy 是否把共享 Host + Header 原样交给 Orchestrator；默认关闭时会改写为 `{port}-{sandboxID}.<DOMAIN_NAME>` 兼容格式。它不是 Header 寻址总开关。

普通 `{port}-{sandboxID}.<DOMAIN_NAME>` Host 始终是权威目标，即使携带 routing Header 也不会被覆盖。共享 Host、localhost 或 IP 上只要出现任一 routing Header，两个 Header 就必须同时存在。

### 6.2 自动恢复与暂停语义

- `ed90bd31f` 为私有化 Client Proxy 增加 API internal/edge gRPC 恢复通道，并转发 private ingress traffic token 与 secure envd access token。
- Client Proxy 在 Redis catalog 未命中、Sandbox 保存了 `autoResume.enabled=true` 且快照可自动恢复时，才会调用 API 恢复。
- `sandbox-auto-resume` 曾在此版本区间用于总开关，但 `f38b3a9fe` 在最终 `2026.28` 中删除了它；当前不能继续配置该 Flag。
- `c09d96ad1`、`ecd13b04d` 增加 filesystem-only pause/auto-pause。`memory=false` 或 `autoPauseMemory=false` 只保存文件系统，恢复时冷启动，且不能由流量自动恢复。

### 6.3 Sandbox 网络配置

- `758e726c7` 增加按域名的 HTTP/HTTPS Header transform，通过 `network.rules` 配置，并受 `network-transform-rules` 门控。
- `1fc3820d7` 增加 `network.egressProxy` SOCKS5 出口，通过 `byop-proxy-enabled` 门控；allow/deny 过滤先执行，域名流量使用 remote DNS。
- `PUT /sandboxes/{sandboxID}/network` 是全量替换，省略字段会清除现有对应配置，不是 merge patch。

### 6.4 列表与过滤 API

- `91e02e48a` 增加 `GET /v2/templates`，使用 `limit`/`nextToken` 游标分页，下一页 token 从响应 Header `X-Next-Token` 读取；旧 `GET /templates` 已标记 deprecated。
- `6fa1bc76b` 为 `GET /snapshots` 增加 `name` 过滤，接受名称、ID、namespace 和可选 tag。

完整调用示例和限制已整理到 [`2026.28用户可见功能.md`](../reference/2026.28用户可见功能.md)。`db8b3747e` 的 envd WebSocket raw TCP tunnel 与 `1b19a3bcb` 的动态日志路由不属于 `2026.28` 标签，本次不将其声明为当前能力。

---

## 7. Feature Flags 变化

从 `2026.17` 到 `2026.28` 新增的 flags：

```text
build-kernel-version
byop-proxy-enabled
clickhouse-read-endpoint
clickhouse-write-fanout
collapse-envd-heap
collapse-envd-heap-timeout-ms
compress-config
disable-e2b-access-token-auth
disable-e2b-access-token-provisioning
envd-timeout-milliseconds
expiration-index-healer
free-page-hinting-config
free-page-reporting
freeze-user-cgroup
guest-pause-reclaim
guest-sync-timeout-milliseconds
header-v5-write
max-concurrent-evictions
max-parallel-build-read-segments
max-starting-instances-per-node
memfd-background-copy
memfile-diff-dedup
min-chunker-read-size-kb
nbd-async-write-zeroes
network-transform-rules
orch-accepts-combined-host
pause-resume-prefetch-consume
pause-resume-prefetch-harvest
pause-resume-prefetch-harvest-timeout-ms
resume-origin-node-remap
sandbox-volume-label-based-scheduling
storage-soft-delete-check
storage-soft-delete-enforce
use-memfd
v4-header-for-uncompressed
volume-fallback-to-unmatched-nodes
```

从 `2026.28` 代码中消失的旧 flags：

```text
best-of-k-can-fit
best-of-k-too-many-starting
chunker-config
edge-provided-sandbox-metrics
execution-metrics-on-webhooks
host-stats-sampling-interval
sandbox-auto-resume
sandbox-metrics-read
sandbox-metrics-write
```

已完成校准：

- `FeatureFlags私有化部署方案.md` 已以 `infra/packages/shared/pkg/featureflags/flags.go` 为源校准名称、类型和 fallback，共 80 项。
- 内存、暂停/恢复和压缩相关 flag 已按源码用途说明，不把 fallback 当作独立运行时配置文件。
- 认证迁移 flag 已保留，YAML、Unleash 与 `FEATURE_FLAGS_PROVIDER` 明确标为需要代码改造，而非当前能力。

---

## 8. 数据库与迁移风险

`2026.17 → 2026.28` 的 DB/ClickHouse 变更不能忽略。重点类别：

- public users / user identities：
  - `20260515120000_create_user_identities_table.sql`
  - `20260520193000_deprecate_public_users_email.sql`
  - `20260521181000_drop_public_users_email.sql`
- cluster / auth org：
  - `20260423170000_cluster_auth_org_id.sql`
  - `20260609120000_cluster_name.sql`
- env/template/snapshot：
  - `20260603120000_add_envs_team_source_created_at_index.sql`
  - `20260612120000_add_envs_team_updated_at_partial_index.sql`
  - `20260612140000_add_env_builds_id_covering_index.sql`
  - `20260628120000_add_env_deleted_at.sql`
- TTL：
  - Postgres: `20260702120000_add_events_ttl_days.sql`
  - ClickHouse: `20260702120000_add_sandbox_events_ttl_days.sql`
- ClickHouse webhook/event：
  - `20260512185000_add_webhook_deliveries.sql`

已完成校准：

- 手工部署文档已使用 `make -C infra/packages/db migrate`，并将 tracking 表修正为 `_migrations`。
- Kubernetes 文档已改用 `infra/packages/db/Dockerfile` 构建的 `./migrator` 镜像，不再调用不存在的 API `./migrate`。
- ClickHouse 文档已区分硬编码本机连接的 `migrate-local` 与使用实际连接串、`_migrations` tracking 表的远端 migration；官方 Nomad migrator job 已作为对照核验。

---

## 9. 中文文档更新完成记录

### 第一批：官方入口与当前事实（已完成）

已更新：

- `infra/self-host.md`
- `README.md`
- `启动参数详解.md`

完成结果：

- README 已声明源码与英文文档统一来自固定 `infra/` 子模块。
- 参数文档已增加 `AUTH_PROVIDER_CONFIG`、`SERVICE_DISCOVERY_PROVIDER`、API internal/edge gRPC 与 Ory/OIDC 当前契约。
- `2026.17` 已明确限制为本文历史比较。

### 第二批：专题文档校准（已完成）

已更新：

- `核心组件详解.md`
- `私有化部署组件分析.md`
- `FeatureFlags私有化部署方案.md`（含 LaunchDarkly 离线/默认值说明）
- `K8s私有化部署指南.md`

完成结果：

- 认证已从 Supabase-only 调整为 provider-based。
- K8s 指南已使用上游内置 discovery，并修正 label、RBAC、HostIP/固定端口和 migration 示例。
- Feature Flags 清单已按 `2026.28` 校准为 80 项。
- LaunchDarkly 离线 fallback 与 YAML/Unleash 改造边界已明确。

### 第三批：无 IaC 与 HA 部署路径（已完成）

已更新：

- `不修改代码完整部署指南.md`
- `不修改代码高可用部署方案.md`
- `Ansible部署指南.md`

完成结果：

- 手工部署已对齐当前 env、端口、服务发现、Redis/Postgres/ClickHouse migration 和 secret 条件。
- Client Proxy 文档已说明 internal 地址优先、edge TLS/OAuth 回退和两者为空时的行为。
- Redis 已明确为 API 与 Client Proxy 启动必需，不再描述内存 catalog。
- 本地“零 Terraform / 零 IaC”定位和 Ansible 增强资产仍保留，未修改上游运行时代码或 IaC。

### 第四批：用户可见功能（已完成）

已更新：

- `2026.28用户可见功能.md`
- `不修改代码完整部署指南.md`
- `K8s私有化部署指南.md`
- `Ansible部署指南.md`
- `启动参数详解.md`
- `核心组件详解.md`
- `FeatureFlags私有化部署方案.md`

完成结果：

- Host/Header 两种 Sandbox 寻址、普通 Host 优先级、共享 Host 兼容改写和 IP 入口限制已写入部署与架构文档。
- DNS、TLS、Nginx 和 K8s Ingress 已明确区分通配 Host 与 `sandbox.<DOMAIN_NAME>` 精确共享入口。
- 自动恢复、网络 transform、SOCKS5 出口、filesystem-only 暂停、模板分页和 snapshot 名称过滤已按 OpenAPI 与 handler 补充。
- 不在正式标签中的 WebSocket raw TCP tunnel 和动态日志路由已明确排除。

---

## 10. 验证命令

当前仓库实际基线核验：

```bash
git submodule status infra
git -C infra rev-parse HEAD
git -C infra describe --tags --exact-match HEAD
git -C infra status --short
git -C infra diff --shortstat 2026.17..2026.28
```

预期 SHA 为 `fda7bef1095afb909197e272c0a8a123797f0bfb`、tag 为 `2026.28`，子模块内部无输出。本文的上游差异证据也可在临时克隆中重新生成：

```bash
git ls-remote https://github.com/e2b-dev/infra.git refs/tags/2026.17 refs/tags/2026.28
```

```bash
tmp=$(mktemp -d)
git clone --filter=blob:none --no-checkout https://github.com/e2b-dev/infra.git "$tmp/infra"
cd "$tmp/infra"
git fetch --tags --filter=blob:none
git diff --shortstat 2026.17..2026.28
git diff --name-status 2026.17..2026.28 -- '*.md' '*.mdx' AGENTS.md CLAUDE.md README.md self-host.md docs
```

配置事实核对：

```bash
git diff --unified=0 2026.17..2026.28 -- \
  self-host.md \
  .env.gcp.template \
  .env.aws.template \
  packages/api/internal/cfg/model.go \
  packages/client-proxy/internal/cfg/model.go \
  packages/dashboard-api/internal/cfg/model.go \
  packages/shared/pkg/featureflags/flags.go
```

Feature Flags 差异核对：

```bash
for ref in 2026.17 2026.28; do
  git show "$ref:packages/shared/pkg/featureflags/flags.go" |
    rg -UP -o --replace '$1' \
      '(?:New|new)(?:Bool|Int|JSON|String|Float)Flag\(\s*"([^"]+)"' |
    sort > "$tmp/$ref.flags"
done
comm -13 "$tmp/2026.17.flags" "$tmp/2026.28.flags"
comm -23 "$tmp/2026.17.flags" "$tmp/2026.28.flags"
```

本地文档更新后的旧契约自查；除本文明确标注的历史比较外，当前文档不应使用这些接口：

```bash
rg "SUPABASE_JWT_SECRETS|API_GRPC_ADDRESS|API_GRPC_PORT|ALLOW_SANDBOX_INTERNET|IP_SLOT_STORAGE|goose_db_version|NewMemorySandboxesCatalog" \
  README.md docs \
  -g '!docs/upstream/上游同步说明-2026.17-to-2026.28.md' \
  -g '!docs/daytona/**'
```

```bash
rg "2026\\.28|AUTH_PROVIDER_CONFIG|SERVICE_DISCOVERY_PROVIDER|API_INTERNAL_GRPC_ADDRESS|ORY_SDK_URL|VOLUME_TOKEN_ENABLED" \
  README.md docs/reference docs/architecture docs/deployment docs/feature-flags docs/upstream
```

用户可见功能边界核对：

```bash
git -C infra merge-base --is-ancestor 345fa2452 2026.28
git -C infra merge-base --is-ancestor f50b5f281 2026.28
git -C infra merge-base --is-ancestor 1fc3820d7 2026.28
git -C infra merge-base --is-ancestor 91e02e48a 2026.28
git -C infra merge-base --is-ancestor db8b3747e 2026.28 && echo unexpected

rg "E2b-Sandbox-Id|E2b-Sandbox-Port|sandbox\\.<DOMAIN_NAME>|X-Next-Token|egressProxy|autoPauseMemory" \
  README.md docs/reference docs/architecture docs/deployment docs/feature-flags docs/upstream
```

最终范围与格式检查：

```bash
git diff --check
git status --short
git diff --name-only
```

---

## 11. 已执行范围与边界

本次已完成：

- 将 `infra/` 子模块固定到 `2026.28` 的完整提交。
- 逐项校准 README、参数、架构、Feature Flags、用户可见功能、Kubernetes、裸机/HA 和 Ansible 中文文档。
- 对照 `self-host.md`、环境模板、服务配置模型、Feature Flags、Nomad job 和 migrations 复核关键事实。

本次没有执行：

- 不修改 Terraform、Nomad job、Ansible 资产、Go 代码或 migration。
- 不复制子模块中的英文文档到根仓库。
- 不把 YAML、Unleash、Redis slot backend 等设想声明为上游已有能力。
- 不修改 Daytona 专题文档。

因此本文文件名继续保留 `2026.17-to-2026.28` 以维持外部链接，但当前事实基线已经是 `2026.28`，不是待执行计划。

---

*同步完成于固定上游 tag 2026.28，commit fda7bef1095afb909197e272c0a8a123797f0bfb*
