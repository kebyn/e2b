# E2B Dashboard 环境变量

# API 地址
NEXT_PUBLIC_API_URL=http://{{ api_domain }}:{{ dashboard_api_port }}

# 旧 dashboard 前端兼容变量；API/Dashboard API 认证使用 AUTH_PROVIDER_CONFIG/Ory
NEXT_PUBLIC_SUPABASE_URL={{ supabase_url }}
NEXT_PUBLIC_SUPABASE_ANON_KEY={{ supabase_anon_key }}

# 环境
NODE_ENV=production
